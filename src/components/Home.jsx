import React from 'react';
import Header from './Header';
import { Outlet } from 'react-router-dom';

const Home = () => {
    return (
        <div>
          <h1>This is home</h1>
        </div>
    );
};

export default Home;